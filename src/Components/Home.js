import Transactions from "./Transactions";

const Home = () => {
  return (
    <div>
      <h1 className="header">Bank Account Total: ${Number}</h1>
      <Transactions />
    </div>
  );
};

export default Home;
