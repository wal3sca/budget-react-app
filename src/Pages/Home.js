import Transactions from "../Components/Transactions";

const Home = () => {
  return (
    <div>
      <h1 className="header">Bank Account Total: ${Number}</h1>
      <Transactions />
    </div>
  );
};

export default Home;
